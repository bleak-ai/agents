# Diagrams

Finished diagrams, one SVG per file, each with a metadata header comment
(archetype, claim, source, audience, date, notes). The wireframe step
consults this index for reusable compositions.

- `gcontext-architecture.svg`: pipeline-flow; runtimes do the reasoning, the agent folder keeps the state, the MCP server connects the two.
