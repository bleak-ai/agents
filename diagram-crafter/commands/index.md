# Commands

User-invokable entry points, exposed as slash commands. Craft is the main
action; setup runs once after install.

- `craft.md`: create a diagram from a text; the main command.
- `setup.md`: verify seeds and report render availability.
