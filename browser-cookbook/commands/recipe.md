---
description: Run a saved recipe
each: recipes/*/
---

Execute the saved recipe `$each`. `$each` is the recipe folder name (for example export-contact-list). Follow the procedure in `commands/run_recipe.md` (read it with read_file), skipping its step 1: the recipe is `$each`, do not ask which one. Use the argument values given below; a parameter left empty takes its stated default; ask only for required parameters that are still missing.
